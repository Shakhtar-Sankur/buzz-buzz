node_modules/
.tools/
dist/
.env
.env.local
.DS_Store
*.log
*.tsbuildinfo
android/.gradle/
android/**/build/
android/local.properties
android/app/google-services.json
*.keystore
*.jks
*.aab
*.apk
*.zip

# Simulated-driver harness credentials (test project keys)
agents/.env.agents

# Superseded by Populace, which now lives in its own repository.
agents/

# editor/tooling config
.claude/

# Test-project credentials for Populace runs. Never committed.
.env.test

# The published build. *.apk above keeps every Gradle output out of the repo,
# which is right — but the one under docs/ is the download the website offers,
# so it is deliberately tracked. Negations must come after the pattern they
# undo, which is why this sits at the end of the file.
!docs/*.apk
